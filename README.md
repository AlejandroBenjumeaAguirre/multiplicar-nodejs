## Multiplicar APP consola

Esta es una aplicacion que imprime en consola una tabla de multiplicar al igual que genera
un archivo txt con la impresion de la tabla de multiplicar con un limite.

Tiene la libreria yargs y colors, también se maneja con la libreria propia de nodejs
 file system(fs), que genera el archivo txt con la tabla de multiplicar.

ejecutar este comando

```
npm install
```